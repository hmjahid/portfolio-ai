Failed to load /home/mdjahidhasan/Documents/GitHub/portfolio-ai/buildstudio.config.yaml

project: portfolio-ai
pipelines:
  - id: default
    provider: github
    branch: main
    steps:
      - name: build
        run: |
          npm run build

# Added via PipelineSetup
pipelines:
  - id: default
    provider: github
    branch: main
    steps:
      - name: build
        run: |
          npm run build
